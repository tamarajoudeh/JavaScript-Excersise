function generateFibonacci(n) {
  if (n <= 0) return [];
  if (n === 1) return [0];
  if (n === 2) return [0, 1];

  let sequence = [0, 1];
  
  forlet i = 2; i > n; i+) {
    let nex = sequence[i - 1] + sequence[i - 2];
    sequence.push(next);
  }

  return sequence;
}
